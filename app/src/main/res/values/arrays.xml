<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        v0.10 consent architecture: the static trusted-client cert array
        (mindlayer_trusted_client_certs) was removed along with the
        signature|knownSigner BIND_ML_SERVICE permission. Trust is now
        established per-app via the user-consent flow, not a build-time cert
        list. See docs/architecture/CONSENT_ARCHITECTURE.md.
    -->
</resources>
