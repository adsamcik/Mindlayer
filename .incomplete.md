# Incomplete work — Mindlayer

Status as of 2026-06-26. The repo is otherwise clean: `main` is in sync with
`origin/main` (ahead 0 / behind 0), working tree clean, no stashes.

## Open item: stale local branch awaiting a decision

Branch **`refactor/remove-deprecated-api`** (commit `8377a7d` —
"refactor(sdk): remove HIDDEN deprecated OCR alias methods"):

- It is **not merged into `main`** (contains 1+ unmerged commit).
- Its upstream tracking branch is **gone** — `origin/refactor/remove-deprecated-api`
  was deleted on the remote (`git branch -vv` shows `[origin/...: gone]`).

This was left untouched on purpose: pushing it would recreate a deleted remote
branch, and merging it into `main` is a judgment call — either action could undo
an intentional cleanup (e.g. a closed/superseded PR).

**Decide one of:**
- **leave** — delete the local branch if it is obsolete (`git branch -D refactor/remove-deprecated-api`)
- **push** — republish it (`git push -u origin refactor/remove-deprecated-api`) and open a PR
- **merge** — fast-forward/merge into `main` and push, if the change is wanted

Inspect first: `git show 8377a7d` and `git log main..refactor/remove-deprecated-api`.
